# AGENTS.md

## 環境
- Pythonのライブラリ導入は uv のみ。pip でグローバルに入れない。

## 実装時に気をつけること
- UIモックなどの一時的なファイルはプロジェクトルートにtmpディレクトリを作成し,そこへ保存すること.
- ユーザーに提示するためのモックやドキュメントとして作成したHTMLは、Chrome拡張機能で開き、ユーザーがその内容を直接確認できる状態で提示すること。

## run_batch.sh変更時の検証
- `run_batch.sh` を変更した場合は、Windowsローカルで実行できる範囲に限り、次の検証を毎回行うこと。
  - Git Bashで `bash -n run_batch.sh` を実行する。
  - 埋め込みPythonコードを抽出し、Pythonの `compile()` で構文を確認する。
  - ライブプレビューHTML内のJavaScriptを抽出し、Node.jsで構文を確認する。
  - 変更した計算や状態遷移について、外部サービスへ接続しない単体テストを行う。完了通知を変更した場合は、完了時の一度だけの発火、同じ完了状態での重複防止、ページ再読み込み相当での再発火防止を確認する。時間表示を変更した場合は、個別時間、完了済み件数だけを母数にした平均、`images/s` の計算結果を確認する。
  - `git diff --check -- run_batch.sh` を実行する。
- Vast.ai、ComfyUI、GPU、Cloudflare Tunnelなどを使う実環境テストや実画像生成は行わないこと。
- Chromeなどの実ブラウザを起動・操作してテストしないこと。ユーザーが明示的に依頼した場合を除き、既に開いているブラウザやUIにも触らないこと。
- 検証結果を報告する際は、Windowsローカルで行った検証だけを記載し、実環境で確認したと誤解される表現を使わないこと。
- 上記の検証がすべて成功し、差分レビューでも問題がなければ、ブランチは作成・切り替えず、今回変更した対象ファイルだけをコミットして `origin/main` へプッシュすること。作業ツリーにあるユーザーの別変更をコミットへ混ぜないこと。
- プッシュ成功後は、ユーザーがJupyter WebUIのターミナルで更新を取得して動作確認できるよう、少なくとも次のコマンドを提示すること。

  ```bash
  cd /workspace/qwen_comfy_sh
  git pull --ff-only origin main
  bash /workspace/qwen_comfy_sh/run_batch.sh
  ```

## やってはいけないこと
- VLMを用いて画像の解析を行わない.リポジトリ内の画像には一切触れない
- 設定ファイルを無断で上書きしない
- ログやprintに絵文字を使わない。一般的な日本語、英語のみ。

##　デザインについて
- HTML/CSS を用いた GUI・レポート・Web UI のデザインでは、登録済みの `taste-skill` を使用すること。
